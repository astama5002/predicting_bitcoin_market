# Bitcoin Market Prediction App

This is a Streamlit app that predicts Bitcoin sentiment and price trends using an LSTM model.

To run locally:
1. Install Python
2. pip install -r requirements.txt
3. streamlit run app.py
